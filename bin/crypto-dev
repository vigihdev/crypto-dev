#!/usr/bin/env php
<?php

foreach ([__DIR__ . '/../../../autoload.php', __DIR__ . '/../vendor/autoload.php'] as $file) {
    if (file_exists($file)) {
        require $file;
        break;
    }
}

$argv = $_SERVER['argv'] ?? [];
$arg = array_slice($argv, 1);

if (!isset($arg[0])) {
    exit;
}
